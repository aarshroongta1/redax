import daisyui from 'daisyui'

export default {
  plugins: [
    daisyui({
      base: false, 
      styled: true,
      themes: true,
      utils: true,
      logs: true,
      rtl: false,
    })
  ]
}